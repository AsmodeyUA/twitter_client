Twitter Client API
------------------

The simplest way for starting web app locally run start.sh script*:
    ./start.sh

Or:
    1) create virtual environment
    2) install modules from requirements.txt
    3) start flask server with command:
        FLASK_APP=client_api.py flask run

*python3 and pip3 should be installed before executing any step from this instructions